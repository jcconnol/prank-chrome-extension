chrome.tabs.onUpdated.addListener((tabId, changeInfo, tab) => {
    /*
    chrome.tabs.insertCSS(
        tabId,
        { file: "snackbar.css" },
        () => console.log("loaded css")
    );
    */
});